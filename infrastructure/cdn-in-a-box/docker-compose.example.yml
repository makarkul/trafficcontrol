version: '3.8'

services:
  example:
    build:
      context: example  # Path to the folder containing Dockerfile
      dockerfile: Dockerfile
    ports:
      - "8080:80"  # Map port 80 in the container to port 8080 on the host
    restart: unless-stopped
